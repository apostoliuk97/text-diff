{
  "name": "@diff/lib",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "lerna run clean && tsc -p tsconfig.build.json && cd ./dist && node index.js",
    "build": "lerna run clean && tsc -p tsconfig.build.json",
    "clean": "rm -rf ./dist;",
    "test": "jest",
    "test:coverage": "jest --collect-coverage"
  },
  "dependencies": {
    "@diff/websocket": "1.0.0"
  },
  "devDependencies": {
    "@babel/preset-typescript": "^7.9.0",
    "jest": "^25.3.0"
  }
}
