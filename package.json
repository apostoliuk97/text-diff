{
  "private": true,
  "name": "text-diff-app",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run start --scope={@diff/app,@diff/server}",
    "start:client": "lerna run start --scope=@diff/app",
    "start:server": "lerna run start --scope=@diff/server",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
